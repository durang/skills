# Legacy folders that predate this monorepo — NOT migrated yet.
# If you want to bring them into the system, move to claude/, openclaw/, or shared/
# with a commit. Until then, they stay local and out of the public repo.
landing-gen/
lead-scraper/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Backups
*.bak
*.bak-*
backups/

# Personal / business artifacts that live in this dir but MUST stay local-only.
# Added so `git add -A` accidents never leak them to the public repo.
# (2026-06-03: blindar tras revisión del workspace.)
jpc-*
JPC-*
foreman-*
alteca-*
cronograma-*
fdp-*
*-decision-brief*.md
*-shareable-technical-brief*.md
*-technical-architecture*.md
create_jpc_*.py
tmp/

# Skills that exist locally but haven't been productionized yet (no distribute-to:
# frontmatter aún — no se sube hasta que la migremos formalmente).
shared/reclip/
